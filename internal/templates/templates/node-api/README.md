# pulse starter: node-api

One Node.js function behind `GET /hello`.

```bash
pulse validate                                 # check the config
pulse list                                     # see functions & triggers
pulse start                                    # boot the local environment
pulse invoke hello --event events/hello.json   # run it locally
curl "localhost:3000/hello?name=you"           # once phase 2 lands
```
